# Auto detect text files and perform LF normalization
* text=auto

# Ensure that headers for lua and shader files are not regenerated by forcing their line endings to LF
*.lua text eol=lf
*.fs text eol=lf

# Handle images and fonts as binary
*.png binary
*.ttf binary
